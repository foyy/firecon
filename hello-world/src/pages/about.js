import React from "react";
import Header from "../components/header";

export default () => (
  <div style={{ color: `teal` }}>
    <Header headerText="About Gatsby" />
    <Header headerText="Another header here" />
    <p>Such wow. Very React.</p>
  </div>
);
